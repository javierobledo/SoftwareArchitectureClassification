<%= form_with(model: corpus, local: true) do |form| %>
  <% if corpus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(corpus.errors.count, "error") %> prohibited this corpus from being saved:</h2>

      <ul>
      <% corpus.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :corpus_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :corpus_description %>
  </div>

  <div class="field">
    <%= form.label :language %>
    <%= form.text_field :language, id: :corpus_language %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
